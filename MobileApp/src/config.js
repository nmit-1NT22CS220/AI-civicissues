// Replace with your machine's local IP address if running on a real device
// For Android Emulator, use '10.0.2.2'
// For iOS Simulator, use 'localhost'
const HOST_IP = '192.168.0.109';

export const API_URL = `http://${HOST_IP}:8000`;
export const AI_URL = `http://${HOST_IP}:9000`;
